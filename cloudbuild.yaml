# Fixed cloudbuild.yaml - fully working version
substitutions:
  _REGION: 'us-central1'
  _SERVICE_NAME: 'threat-intelligence-platform'
  _MIN_INSTANCES: '1'
  _MAX_INSTANCES: '10'

steps:
# Step 0: Enable APIs
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk:alpine'
  id: 'setup-apis'
  entrypoint: 'bash'
  args:
  - '-c'
  - |
    set -e
    echo "Enabling required APIs..."
    gcloud services enable \
      cloudbuild.googleapis.com \
      run.googleapis.com \
      storage.googleapis.com \
      bigquery.googleapis.com \
      secretmanager.googleapis.com \
      cloudscheduler.googleapis.com \
      pubsub.googleapis.com \
      logging.googleapis.com \
      clouderrorreporting.googleapis.com \
      aiplatform.googleapis.com

# Step 1: Build image
- name: 'gcr.io/cloud-builders/docker'
  id: 'build'
  args:
  - 'build'
  - '-t'
  - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:$COMMIT_SHA'
  - '-t'
  - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:latest'
  - '.'

# Step 2: Push image
- name: 'gcr.io/cloud-builders/docker'
  id: 'push'
  args:
  - 'push'
  - '--all-tags'
  - 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}'

# Step 3: Setup resources
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk:alpine'
  id: 'setup-resources'
  entrypoint: 'bash'
  args:
  - '-c'
  - |
    set -e
    
    # Create API keys secret
    if ! gcloud secrets describe api-keys >/dev/null 2>&1; then
      echo "Creating api-keys secret..."
      gcloud secrets create api-keys --replication-policy="automatic"
      
      # Generate and store API key
      KEY=$(openssl rand -hex 32)
      echo "{\"platform_api_key\": \"$KEY\"}" | \
        gcloud secrets versions add api-keys --data-file=-
    fi
    
    # Create BigQuery dataset
    if ! bq ls -d | grep -q "threat_intelligence"; then
      bq --location=US mk -d threat_intelligence
    fi
    
    # Create Storage bucket
    BUCKET="${PROJECT_ID}-threat-data"
    if ! gsutil ls | grep -q "gs://${BUCKET}"; then
      gsutil mb -l ${_REGION} "gs://${BUCKET}"
    fi
    
    # Create Pub/Sub topics
    gcloud pubsub topics create threat-data-ingestion 2>/dev/null || true
    gcloud pubsub topics create threat-analysis-events 2>/dev/null || true

# Step 4: Deploy to Cloud Run
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk:alpine'
  id: 'deploy'
  entrypoint: 'bash'
  args:
  - '-c'
  - |
    set -e
    
    gcloud run deploy ${_SERVICE_NAME} \
      --image=gcr.io/$PROJECT_ID/${_SERVICE_NAME}:$COMMIT_SHA \
      --region=${_REGION} \
      --platform=managed \
      --allow-unauthenticated \
      --memory=2Gi \
      --cpu=2 \
      --min-instances=${_MIN_INSTANCES} \
      --max-instances=${_MAX_INSTANCES} \
      --timeout=300s \
      --set-env-vars="ENVIRONMENT=production" \
      --set-env-vars="GCP_PROJECT=$PROJECT_ID" \
      --set-env-vars="GCP_REGION=${_REGION}" \
      --set-env-vars="BIGQUERY_DATASET=threat_intelligence" \
      --set-env-vars="GCS_BUCKET=${PROJECT_ID}-threat-data" \
      --set-env-vars="PUBSUB_TOPIC=threat-data-ingestion" \
      --set-env-vars="PUBSUB_ANALYSIS_TOPIC=threat-analysis-events" \
      --set-env-vars="VERSION=1.0.3" \
      --set-env-vars="DEBUG=false" \
      --set-env-vars="PYTHONPATH=/app" \
      --set-env-vars="LOG_LEVEL=INFO" \
      --set-env-vars="LOG_TO_CLOUD=true" \
      --set-env-vars="ENABLE_ERROR_REPORTING=true" \
      --set-env-vars="NLP_ENABLED=true" \
      --set-env-vars="ANALYSIS_ENABLED=true" \
      --set-secrets="API_KEY=api-keys:latest"

# Step 5: Setup Cloud Scheduler
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk:alpine'
  id: 'setup-scheduler'
  entrypoint: 'bash'
  args:
  - '-c'
  - |
    set -e
    
    # Get service URL
    SERVICE_URL=$(gcloud run services describe ${_SERVICE_NAME} \
      --region=${_REGION} \
      --format='value(status.url)')
    
    # Get API key
    KEY=$(gcloud secrets versions access latest --secret=api-keys | \
      sed -n 's/.*"platform_api_key"[[:space:]]*:[[:space:]]*"\([^"]*\)".*/\1/p')
    
    # Create scheduler jobs
    gcloud scheduler jobs delete threat-data-ingestion --location=${_REGION} --quiet 2>/dev/null || true
    gcloud scheduler jobs delete threat-analysis --location=${_REGION} --quiet 2>/dev/null || true
    
    # Create ingestion job
    gcloud scheduler jobs create http threat-data-ingestion \
      --location=${_REGION} \
      --schedule="0 */6 * * *" \
      --uri="$SERVICE_URL/api/admin/ingest" \
      --http-method=POST \
      --headers="Content-Type=application/json,X-API-Key=$KEY" \
      --message-body='{"process_all": true}' \
      --time-zone="UTC"
    
    # Create analysis job  
    gcloud scheduler jobs create http threat-analysis \
      --location=${_REGION} \
      --schedule="0 2 * * *" \
      --uri="$SERVICE_URL/api/admin/analyze" \
      --http-method=POST \
      --headers="Content-Type=application/json,X-API-Key=$KEY" \
      --message-body='{"analyze_all": true}' \
      --time-zone="UTC"

# Step 6: Verify
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk:alpine'
  id: 'verify'
  entrypoint: 'bash'
  args:
  - '-c'
  - |
    SERVICE_URL=$(gcloud run services describe ${_SERVICE_NAME} \
      --region=${_REGION} \
      --format='value(status.url)')
    
    echo "Service deployed at: $SERVICE_URL"
    sleep 30
    
    if curl -f -s "$SERVICE_URL/health" >/dev/null; then
      echo "Health check passed!"
    else
      echo "Health check failed (service may still be starting)"
    fi

images:
- 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:$COMMIT_SHA'
- 'gcr.io/$PROJECT_ID/${_SERVICE_NAME}:latest'

timeout: '3600s'
options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'E2_HIGHCPU_8'
