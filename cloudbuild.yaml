steps:
  # Install dependencies and run checks
  - name: 'python:3.10'
    id: 'setup-and-checks'
    script: |
      echo "Installing dependencies and running basic checks..."
      pip install --upgrade pip
      pip install --no-cache-dir -r requirements.txt
      
      # Verify config module loads correctly
      python -c "import config; print('Config module loaded successfully')"

  # Build frontend assets
  - name: 'node:16'
    id: 'build-frontend'
    script: |
      echo "Building frontend assets..."
      
      # Create directories and files
      mkdir -p static/src static/dist templates
      echo '@tailwind base; @tailwind components; @tailwind utilities;' > static/src/input.css
      
      # Set up package.json for Tailwind
      cat > package.json << 'EOF'
      {
        "name": "threat-intelligence-platform",
        "version": "1.0.0",
        "scripts": {
          "build": "tailwindcss -i static/src/input.css -o static/dist/output.css --minify"
        },
        "devDependencies": {
          "tailwindcss": "^3.3.0",
          "postcss": "^8.4.21",
          "autoprefixer": "^10.4.14"
        }
      }
      EOF
      
      # Set up Tailwind config
      cat > tailwind.config.js << 'EOF'
      module.exports = {
        content: ["./templates/**/*.html"],
        theme: {
          extend: {
            colors: {
              primary: '#0d6efd',
              secondary: '#6c757d',
              success: '#198754',
              info: '#0dcaf0',
              warning: '#ffc107',
              danger: '#dc3545',
              light: '#f8f9fa',
              dark: '#212529'
            }
          }
        },
        plugins: []
      };
      EOF
      
      # Install dependencies and build CSS
      npm install
      npx tailwindcss -i static/src/input.css -o static/dist/output.css --minify

  # Setup GCP resources and permissions
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'setup-gcp'
    script: |
      echo "Setting up Google Cloud resources..."
      
      # Define project ID and service accounts
      PROJECT_ID="primal-chariot-382610"
      BUILD_SERVICE_ACCOUNT="cloud-build-service@${PROJECT_ID}.iam.gserviceaccount.com"
      
      # Enable required APIs
      echo "Enabling required GCP APIs..."
      gcloud services enable \
        run.googleapis.com \
        cloudbuild.googleapis.com \
        secretmanager.googleapis.com \
        cloudresourcemanager.googleapis.com \
        clouderrorreporting.googleapis.com \
        pubsub.googleapis.com \
        storage.googleapis.com \
        bigquery.googleapis.com \
        iam.googleapis.com \
        aiplatform.googleapis.com
      
      # Create and configure Secret Manager secrets
      echo "Setting up Secret Manager secrets..."
      for SECRET_NAME in api-keys auth-config admin-initial-password feed-config flask-secret-key; do
        # Check if secret exists
        if ! gcloud secrets describe $SECRET_NAME --project=${PROJECT_ID} 2>/dev/null; then
          echo "Creating secret: $SECRET_NAME"
          gcloud secrets create $SECRET_NAME --project=${PROJECT_ID}
          
          # Add initial values based on secret type
          if [[ "$SECRET_NAME" == "api-keys" && $(gcloud secrets versions list $SECRET_NAME --format="value(name)" | wc -l) -eq 0 ]]; then
            API_KEY=$(openssl rand -hex 24)
            echo "{\"platform_api_key\": \"${API_KEY}\", \"created_at\": \"$(date -u +"%Y-%m-%dT%H:%M:%SZ")\"}" | \
            gcloud secrets versions add $SECRET_NAME --data-file=- --project=${PROJECT_ID}
          elif [[ "$SECRET_NAME" == "auth-config" && $(gcloud secrets versions list $SECRET_NAME --format="value(name)" | wc -l) -eq 0 ]]; then
            SESSION_SECRET=$(openssl rand -hex 32)
            ADMIN_PASS=$(echo -n "admin" | sha256sum | awk '{print $1}')
            echo "{\"session_secret\": \"${SESSION_SECRET}\", \"users\": {\"admin\": {\"password\": \"${ADMIN_PASS}\", \"role\": \"admin\", \"created_at\": \"$(date -u +"%Y-%m-%dT%H:%M:%SZ")\"}}}" | \
            gcloud secrets versions add $SECRET_NAME --data-file=- --project=${PROJECT_ID}
          elif [[ "$SECRET_NAME" == "admin-initial-password" && $(gcloud secrets versions list $SECRET_NAME --format="value(name)" | wc -l) -eq 0 ]]; then
            echo "admin" | gcloud secrets versions add $SECRET_NAME --data-file=- --project=${PROJECT_ID}
          elif [[ "$SECRET_NAME" == "feed-config" && $(gcloud secrets versions list $SECRET_NAME --format="value(name)" | wc -l) -eq 0 ]]; then
            echo "{\"feeds\": [{\"id\": \"phishtank\", \"name\": \"PhishTank URLs\", \"url\": \"http://data.phishtank.com/data/online-valid.json\", \"description\": \"URLs verified as phishing by PhishTank community\", \"format\": \"json\", \"type\": \"url\", \"update_frequency\": \"daily\", \"enabled\": true}, {\"id\": \"urlhaus\", \"name\": \"URLhaus Malware\", \"url\": \"https://urlhaus.abuse.ch/downloads/csv_recent/\", \"description\": \"Recent malware URLs from URLhaus\", \"format\": \"csv\", \"type\": \"url\", \"update_frequency\": \"daily\", \"enabled\": true}, {\"id\": \"threatfox\", \"name\": \"ThreatFox IOCs\", \"url\": \"https://threatfox.abuse.ch/export/json/recent/\", \"description\": \"Recent indicators from ThreatFox\", \"format\": \"json\", \"type\": \"mixed\", \"update_frequency\": \"daily\", \"enabled\": true}], \"update_interval_hours\": 6, \"default_tags\": [], \"user_agent\": \"ThreatIntelligencePlatform/1.0.0\", \"request_timeout\": 30, \"max_retries\": 3}" | \
            gcloud secrets versions add $SECRET_NAME --data-file=- --project=${PROJECT_ID}
          elif [[ "$SECRET_NAME" == "flask-secret-key" && $(gcloud secrets versions list $SECRET_NAME --format="value(name)" | wc -l) -eq 0 ]]; then
            openssl rand -hex 32 | gcloud secrets versions add $SECRET_NAME --data-file=- --project=${PROJECT_ID}
          fi
        else
          echo "Secret $SECRET_NAME already exists"
        fi
        
        # Grant access to the service account
        echo "Configuring IAM for secret: $SECRET_NAME"
        gcloud secrets add-iam-policy-binding $SECRET_NAME \
          --member="serviceAccount:${BUILD_SERVICE_ACCOUNT}" \
          --role="roles/secretmanager.secretAccessor" \
          --project=${PROJECT_ID}
      done
      
      # Create BigQuery dataset
      echo "Setting up BigQuery dataset..."
      DATASET_ID="threat_intelligence"
      if ! bq ls --project_id=${PROJECT_ID} | grep -q ${DATASET_ID}; then
        bq --location=US mk --dataset \
          --description="Threat Intelligence Platform Data" \
          ${PROJECT_ID}:${DATASET_ID}
        echo "Created BigQuery dataset ${DATASET_ID}"
      else
        echo "BigQuery dataset ${DATASET_ID} already exists"
      fi
      
      # Create GCS bucket
      echo "Setting up Cloud Storage bucket..."
      BUCKET_NAME="${PROJECT_ID}-threat-data"
      if ! gsutil ls -b gs://${BUCKET_NAME} 2>/dev/null; then
        gsutil mb -l us-central1 gs://${BUCKET_NAME}
        gsutil uniformbucketlevelaccess set on gs://${BUCKET_NAME}
        echo "Created GCS bucket ${BUCKET_NAME}"
      else
        echo "GCS bucket ${BUCKET_NAME} already exists"
        gsutil uniformbucketlevelaccess set on gs://${BUCKET_NAME}
      fi
      
      # Set up bucket folder structure
      echo "Creating bucket folder structure..."
      for FOLDER in feeds raw processed exports temp cache; do
        mkdir -p /tmp/bucket_folders
        touch /tmp/bucket_folders/empty.txt
        gsutil cp /tmp/bucket_folders/empty.txt gs://${BUCKET_NAME}/${FOLDER}/
        gsutil rm gs://${BUCKET_NAME}/${FOLDER}/empty.txt
      done
      rm -rf /tmp/bucket_folders
      
      # Create PubSub topics
      echo "Setting up Pub/Sub topics..."
      for TOPIC in threat-data-ingestion threat-analysis-events; do
        if ! gcloud pubsub topics describe ${TOPIC} --project=${PROJECT_ID} 2>/dev/null; then
          gcloud pubsub topics create ${TOPIC} --project=${PROJECT_ID}
          echo "Created PubSub topic ${TOPIC}"
        else
          echo "PubSub topic ${TOPIC} already exists"
        fi
        
        # Configure topic IAM permissions
        gcloud pubsub topics add-iam-policy-binding ${TOPIC} \
          --project=${PROJECT_ID} \
          --member="serviceAccount:${BUILD_SERVICE_ACCOUNT}" \
          --role="roles/pubsub.publisher"
          
        gcloud pubsub topics add-iam-policy-binding ${TOPIC} \
          --project=${PROJECT_ID} \
          --member="serviceAccount:${BUILD_SERVICE_ACCOUNT}" \
          --role="roles/pubsub.subscriber"
      done
      
      echo "GCP resource setup completed successfully"

  # Build container
  - name: 'gcr.io/cloud-builders/docker'
    id: 'build-container'
    args: [
      'build',
      '--cache-from', 'gcr.io/primal-chariot-382610/threat-intelligence-platform:latest',
      '-t', 'gcr.io/primal-chariot-382610/threat-intelligence-platform:latest',
      '-t', 'gcr.io/primal-chariot-382610/threat-intelligence-platform:$COMMIT_SHA',
      '.'
    ]

  # Push container to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    id: 'push-container'
    args: [
      'push', 
      'gcr.io/primal-chariot-382610/threat-intelligence-platform:latest'
    ]

  # Deploy to Cloud Run with secrets properly mounted
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'deploy-cloudrun'
    script: |
      echo "Deploying to Cloud Run..."
      
      # Define project ID
      PROJECT_ID="primal-chariot-382610"
      SERVICE_ACCOUNT="cloud-build-service@${PROJECT_ID}.iam.gserviceaccount.com"
      
      # Create YAML configuration file
      cat > cloudrun-config.yaml << EOF
      apiVersion: serving.knative.dev/v1
      kind: Service
      metadata:
        name: threat-intelligence-platform
        namespace: '561511062593'
        annotations:
          run.googleapis.com/client-name: "cloud-console"
          run.googleapis.com/ingress: all
          run.googleapis.com/ingress-status: all
        labels:
          app: threat-intelligence-platform
          environment: production
          managed-by: cloudbuild
      spec:
        template:
          metadata:
            annotations:
              autoscaling.knative.dev/minScale: "1"
              autoscaling.knative.dev/maxScale: "20"
              run.googleapis.com/cpu-throttling: "false"
              run.googleapis.com/startup-cpu-boost: "true"
              run.googleapis.com/execution-environment: "gen2"
            labels:
              app: threat-intelligence-platform
              managed-by: cloudbuild
              run.googleapis.com/startupProbeType: Default
          spec:
            serviceAccountName: "${SERVICE_ACCOUNT}"
            timeoutSeconds: 300
            containerConcurrency: 80
            containers:
            - image: gcr.io/${PROJECT_ID}/threat-intelligence-platform:latest
              ports:
              - name: http1
                containerPort: 8080
              resources:
                limits:
                  cpu: "2"
                  memory: "2Gi"
              env:
              - name: GCP_PROJECT
                value: "${PROJECT_ID}"
              - name: ENVIRONMENT
                value: "production"
              - name: GCP_REGION
                value: "us-central1"
              - name: BIGQUERY_DATASET
                value: "threat_intelligence"
              - name: GCS_BUCKET
                value: "${PROJECT_ID}-threat-data"
              - name: PUBSUB_TOPIC
                value: "threat-data-ingestion"
              - name: PUBSUB_ANALYSIS_TOPIC
                value: "threat-analysis-events"
              - name: VERSION
                value: "1.0.2"
              - name: DEBUG
                value: "false"
              - name: PYTHONPATH
                value: "/app"
              - name: CLOUD_LOGGING
                value: "true"
              - name: ENABLE_ERROR_REPORTING
                value: "true"
              - name: LOG_LEVEL
                value: "INFO"
              - name: LOAD_SECRETS
                value: "true"
              - name: ENSURE_GCP_RESOURCES
                value: "true"
              - name: IGNORE_PERMISSION_ERRORS 
                value: "false"
              # Secret references
              - name: API_KEY
                valueFrom:
                  secretKeyRef:
                    name: api-keys
                    key: latest
              - name: SECRET_KEY
                valueFrom:
                  secretKeyRef:
                    name: auth-config
                    key: latest
              startupProbe:
                tcpSocket:
                  port: 8080
                periodSeconds: 240
                failureThreshold: 1
                timeoutSeconds: 240
              livenessProbe:
                httpGet:
                  path: /health
                  port: 8080
                periodSeconds: 30
                timeoutSeconds: 5
                failureThreshold: 3
              # Readiness probe removed - not available for this project
              volumeMounts:
              - name: tmp
                mountPath: /tmp
              - name: app-data
                mountPath: /app/data
              - name: api-keys-volume
                mountPath: /app/secrets/api-keys
                readOnly: true
              - name: auth-config-volume
                mountPath: /app/secrets/auth-config
                readOnly: true
              - name: feed-config-volume
                mountPath: /app/secrets/feed-config
                readOnly: true
              - name: admin-password-volume
                mountPath: /app/secrets/admin-password
                readOnly: true
            volumes:
            - name: tmp
              emptyDir: {}
            - name: app-data
              emptyDir: {}
            - name: api-keys-volume
              secret:
                secretName: api-keys
                items:
                - key: latest
                  path: api-keys.json
            - name: auth-config-volume
              secret:
                secretName: auth-config
                items:
                - key: latest
                  path: auth-config.json
            - name: feed-config-volume
              secret:
                secretName: feed-config
                items:
                - key: latest
                  path: feed-config.json
            - name: admin-password-volume
              secret:
                secretName: admin-initial-password
                items:
                - key: latest
                  path: admin-password.txt
        traffic:
        - percent: 100
          latestRevision: true
      EOF
      
      # Deploy to Cloud Run
      gcloud run services replace cloudrun-config.yaml \
        --region us-central1 \
        --project=${PROJECT_ID}
      
      # Get Cloud Run URL
      CLOUD_RUN_URL=$(gcloud run services describe threat-intelligence-platform --region=us-central1 --project=${PROJECT_ID} --format='value(status.url)')
      echo "Deployed to: $CLOUD_RUN_URL"

  # Verify deployment
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'verify-deployment'
    script: |
      echo "Verifying deployment..."
      
      # Define project ID
      PROJECT_ID="primal-chariot-382610"
      
      # Wait for service to be ready
      sleep 45
      service_url=$(gcloud run services describe threat-intelligence-platform --region=us-central1 --project=${PROJECT_ID} --format='value(status.url)')
      echo "Service deployed at: ${service_url}"
      
      # Check health endpoint
      max_attempts=10
      for attempt in $(seq 1 $max_attempts); do
        echo "Health check attempt $attempt of $max_attempts..."
        response_code=$(curl -s -o /dev/null -w "%{http_code}" "${service_url}/health" || echo "failed")
        if [ "$response_code" = "200" ]; then
          echo "✅ Health check successful!"
          break
        else
          echo "⚠️ Health check returned: $response_code - waiting 30s..."
          sleep 30
        fi
      done
      
      # Output information about the deployed service
      echo "===================== DEPLOYMENT SUMMARY ====================="
      echo "Service URL: ${service_url}"
      echo "Project: ${PROJECT_ID}"
      echo "Service Account: ${SERVICE_ACCOUNT}"
      echo "Deployed Image: gcr.io/${PROJECT_ID}/threat-intelligence-platform:latest"
      echo "=============================================================="

# Build timeout and options
timeout: '1800s'
options:
  logging: CLOUD_LOGGING_ONLY
  machineType: 'E2_HIGHCPU_8'
